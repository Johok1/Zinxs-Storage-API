
server:
  port: 444
  ssl:
    key-store: keystore.jks
    key-store-password: password
    keyStoreType: PKCS12
    keyAlias: 1
  error:
    include-message: always
    include-binding-errors: always
config:
  application:
    jwtSecret: ======================BezKoder=Spring===========================
    jwtExpirationMs: 86400000

spring:

  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 60000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: zinxshosting@gmail.com
    password: vocqmvfysvrjoivp

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

